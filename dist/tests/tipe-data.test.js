"use strict";
describe('Data Type', () => {
    it("should must declare", function () {
        let name = "Jefry Kurniawan";
        let balance = 10000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 100;
        // balance = "juki";
        // isVip = 100;
    });
});
